<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mainLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.mjmtools.MainActivity$PlaceholderFragment" >

    <ImageView
        android:id="@+id/mainLayoutImage"
        android:layout_width="96dp"
        android:layout_height="96dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:contentDescription="@string/appName"
        android:src="@drawable/ic_launcher" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="@id/mainLayoutImage"
        android:layout_alignBottom="@id/mainLayoutImage"
        android:layout_alignParentRight="true"
        android:layout_alignTop="@id/mainLayoutImage"
        android:layout_toRightOf="@id/mainLayoutImage"
        android:gravity="center"
        android:text="@string/appName"
        android:textSize="24sp" />

    <TextView
        android:id="@+id/mainLayoutAboutText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentRight="true"
        android:layout_below="@id/mainLayoutImage"
        android:paddingLeft="8dp"
        android:paddingRight="8dp"
        android:textSize="16sp" />

    <Button
        android:id="@+id/mainLayoutSettingsButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/mainLayoutAboutText"
        android:onClick="onMainLayoutSettingsButtonClicked"
        android:text="@string/mainLayoutSettingsButtonText" >
    </Button>

</RelativeLayout>